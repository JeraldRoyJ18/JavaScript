//multiple ways od declaring the string
const name = "jerald";
const newName = new String("Roy")

const repoCount = 40;

// console.log(name + repoCount + "value");
//the above way of concating things using strings is kinda old

//instead use string interpolation

console.log(`${name} ${repoCount} value`)




const score = 300 //this is the general way of defining the number in js
//but sometimes we want do it explicity saying that this should be number only 
//in that case we use object like

const balance = new Number(400)